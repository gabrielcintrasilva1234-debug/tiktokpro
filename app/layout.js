export const metadata = { title: "TKPRO - Comunidade TikTok Ads" };

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body style={{ margin: 0, background: "#0B0B0F" }}>{children}</body>
    </html>
  );
}
